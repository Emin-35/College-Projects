How to setup the enviroment:
For final.py
First you need to download the necessasary libraries such as NLTK numpy and re
You can open up the terminal and write these commands

pip install nltk
pip install numpy

For re you don't need to download anything it's python's library

Then you can run the code in Visual Studio Code or any other Python supporting IDE


For Final.ipynb
Open the file in Google Colab
Then run the every section from start to finish one by one
Then you can run the code without any problem